const { RichEmbed } = require("discord.js");

module.exports = {
    name: "breadman",
    category: "general",
    description: "BREAD BREAD BREAD BREAD AAAAAAAAAA",
    run: async(client, message, args) => {
        const msg = await message.channel.send(":womans_hat:\n:breadflush:\n:shirt:\n:jeans:\n:ballet_shoes:");
    }
};
